export interface MatrixLogin {
    access_token: string;
    home_server: string;
    user_id: string;
    device_id: string;
}

export interface UserCredentials {
    user_name: string;
    password: string;
}
